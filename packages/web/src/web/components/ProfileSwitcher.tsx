/**
 * ProfileSwitcher.tsx — compatibility shim.
 * Component moved to OrgSwitcher. Re-exports to avoid import churn.
 */
export { OrgSwitcher as ProfileSwitcher, ProfileAvatar } from './OrgSwitcher';
